# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                   AI生产力专栏 → PPT(SVG) 流水线                          ║
# ╚══════════════════════════════════════════════════════════════════════════╝

name: AI生产力专栏转PPT
description: 将 AI 生产力专栏文章（已有内容或新生成内容）转换为可编辑的 SVG 幻灯片。
icon: "🪄"

input:
  task_mode:
    type: string
    description: 任务模式（existing=把已有内容转PPT，new=先写新文再转PPT）
    required: false
    default: existing
  topic_or_requirement:
    type: string
    description: 写作主题或转换要求
    required: true
  article_content:
    type: string
    description: 已有文章正文（task_mode=existing 时优先使用）
    required: false
  style:
    type: string
    description: 幻灯片风格（极简主义/商务咨询/科技暗黑/瑞士平面/品牌蓝）
    required: false
    default: 商务咨询

steps:
  - skill: ai-productivity-column
    name: 专栏内容准备
    prompt: |
      任务模式：{{input.task_mode}}
      用户要求：{{input.topic_or_requirement}}

      如果下方“已有文章内容”不为空，请优先基于已有内容重写为适合演示文稿的 Markdown 结构；
      如果为空，请根据用户要求先生成一篇新的专栏文章。

      已有文章内容：
      {{input.article_content}}

      输出要求：
      1. 只输出最终 Markdown 正文，不要解释
      2. 标题层级清晰（#、##、###）
      3. 内容适合拆分为 8-15 页演示文稿

  - skill: ppt-svg-generator
    name: 转换为SVG幻灯片
    prompt: |
      请将以下 Markdown 内容转换为可导入 PPT 的 SVG 幻灯片，风格使用「{{input.style}}」。

      Markdown 内容：
      {{prev.output}}

      要求：
      1. 输出到 ./ppt-output/ 目录
      2. 文件命名为 slide-01-xxx.svg 递增编号
      3. 结束时明确告知输出目录和生成页数

on_failure: stop

