# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                           周报生成流水线                                    ║
# ╚══════════════════════════════════════════════════════════════════════════╝

name: 周报生成
description: 一键生成工作周报，自动汇总本周工作内容
icon: "📋"

input:
  week_range:
    type: string
    description: 周报时间范围如"2024.1.1-1.7"
    required: true
  department:
    type: string
    description: 部门名称
    required: false
    default: 通用

steps:
  - skill: docx
    name: 周报生成
    prompt: |
      请生成一份工作周报，时间范围为 {{input.week_range}}，部门为 {{input.department}}。

      周报需包含以下内容：
      1. 本周工作概述
      2. 重点完成事项
      3. 遇到的问题与解决方案
      4. 下周工作计划
      5. 需要协调的事项

      请以专业的周报格式输出为 docx 文档。

on_failure: stop
