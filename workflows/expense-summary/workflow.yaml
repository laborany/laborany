# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                         费用汇总工作流                                    ║
# ╚══════════════════════════════════════════════════════════════════════════╝

name: 费用汇总
description: 智能费用分类汇总，生成费用报表
icon: "💰"

input:
  period:
    type: string
    description: 汇总周期如"2024年1月"
    required: true
  categories:
    type: string
    description: 费用类别
    required: false

steps:
  # ── 第一步：智能分类处理指定周期的费用数据 ──
  - skill: expense-assistant
    name: 费用处理
    prompt: |
      处理并智能分类指定周期内的所有费用记录，
      按类别归集金额，识别异常支出。
      汇总周期：{{input.period}}
      费用类别筛选：{{input.categories}}

  # ── 第二步：生成费用汇总报表 ──
  - skill: xlsx
    name: 汇总报表
    prompt: |
      将费用分类汇总结果生成 Excel 报表，
      包含分类小计、总计及占比分析。
      数据来源：{{prev.output}}

on_failure: stop
