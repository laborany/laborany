# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                         股票研报生成流水线                                  ║
# ║                                                                          ║
# ║  示例工作流：从股票代码到完整研报的自动化流程                                 ║
# ╚══════════════════════════════════════════════════════════════════════════╝

name: 股票研报生成流水线
description: 从股票代码到完整研报的自动化流程，包含数据采集、财务分析、报告生成三个步骤
icon: "📈"

# ┌──────────────────────────────────────────────────────────────────────────┐
# │                           输入参数定义                                     │
# └──────────────────────────────────────────────────────────────────────────┘
input:
  stock_code:
    type: string
    description: 股票代码（如 AAPL、TSLA）
    required: true
  analysis_depth:
    type: string
    description: 分析深度（basic/detailed）
    required: false
    default: detailed

# ┌──────────────────────────────────────────────────────────────────────────┐
# │                           执行步骤                                        │
# └──────────────────────────────────────────────────────────────────────────┘
steps:
  - skill: stock-analyzer
    name: 数据采集
    prompt: |
      请获取股票 {{input.stock_code}} 的以下数据：
      1. 基本面数据（市值、PE、PB、ROE 等）
      2. 最近一年的股价走势
      3. 最新的财务报表摘要
      4. 近期重要新闻和公告

      分析深度：{{input.analysis_depth}}

  - skill: financial-report
    name: 财务分析
    prompt: |
      基于上一步获取的数据，进行深度财务分析：

      {{prev.output}}

      请分析：
      1. 盈利能力分析
      2. 成长性分析
      3. 估值水平分析
      4. 风险因素识别
      5. 投资建议

  - skill: pdf
    name: 报告生成
    prompt: |
      将以下分析结果生成一份专业的 PDF 研报：

      {{prev.output}}

      报告要求：
      1. 包含封面、目录、正文、免责声明
      2. 使用专业的金融报告格式
      3. 包含必要的图表和数据表格
      4. 文件名：{{input.stock_code}}_研报_{{date}}.pdf

# ┌──────────────────────────────────────────────────────────────────────────┐
# │                           失败策略                                        │
# └──────────────────────────────────────────────────────────────────────────┘
on_failure: stop
